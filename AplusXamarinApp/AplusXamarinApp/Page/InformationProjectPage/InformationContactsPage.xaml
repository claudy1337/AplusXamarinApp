<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AplusXamarinApp.Page.InformationProjectPage.InformationContactsPage"
             Visual="Default">
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="pencil.png" Priority="0" Order="Primary" x:Name="TEditingContacts" Clicked="TEditingContacts_Clicked"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>

        <ListView x:Name="friendsList" ItemsSource="{Binding}" ItemSelected="friendsList_ItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ViewCell.View>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding UserName}" TextColor="Black" FontSize="Header"/>
                            </StackLayout>
                        </ViewCell.View>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>